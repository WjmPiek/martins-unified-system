# Login logo fix

The login page now renders the Martins logo directly from `app/static/img/logo.png` with a fallback to `logo-placeholder.svg`. CSS keeps the logo crisp, responsive, and proportional on all devices.
